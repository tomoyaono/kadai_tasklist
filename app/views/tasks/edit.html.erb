<h1>id: <%= @tasks.id %> のタスク編集</h1>

<%= form_for(@tasks) do |f| %>
  <%= f.label :content, 'タスクを編集する' %>
  <%= f.text_field :content %>

  <%= f.submit '投稿' %>
<% end %>

<%= link_to '一覧に戻る', tasks_path %>